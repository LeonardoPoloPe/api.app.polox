// Exporta todos os controllers para facilitar importação
const authController = require("./authController");
const userController = require("./userController");

module.exports = {
  authController,
  userController,
};
