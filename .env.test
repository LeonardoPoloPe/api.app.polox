# ==========================================
# 🧪 CONFIGURAÇÃO DE AMBIENTE PARA TESTES
# ==========================================
#
# Este arquivo contém as credenciais para o banco de DESENVOLVIMENTO
# usado nos testes automatizados.
#
# IMPORTANTE:
# - As credenciais são as mesmas do ambiente dev (dev-mysql no AWS Secrets Manager)
# - O banco 'app_polox_test' será criado automaticamente se não existir
# - Todos os testes rodam no banco 'app_polox_test' (nunca em dev/sandbox/prod)
# - Este arquivo NÃO deve ser commitado no Git (.env* está no .gitignore)
#

# Banco de Dados (credenciais do dev-mysql no AWS Secrets Manager)
DB_HOST=database-1.cd2em8e0a6ot.sa-east-1.rds.amazonaws.com
DB_PORT=5432
DB_USER=polox_dev_user
DB_PASSWORD=SenhaSeguraDev123!
DB_NAME=app_polox_test

# JWT Secrets (apenas para testes)
JWT_SECRET=test_jwt_secret_key_for_testing_only_12345678
JWT_REFRESH_SECRET=test_refresh_secret_key_12345
JWT_EXPIRES_IN=24h

# Ambiente
NODE_ENV=test
LOG_LEVEL=error

# Desabilitar serviços externos
SKIP_MIGRATIONS=true
SKIP_RATE_LIMIT=true
REDIS_ENABLED=false
SENTRY_DSN=
